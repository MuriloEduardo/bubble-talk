app.controller('suaContaCtrl', function($scope, $rootScope){
	
	// Variavel Scope root responsavel por informar se 
	// Menu a esquerda e seus botoes controladores
	// Aparecerão ou não
	$rootScope.menuLeft = false;
});